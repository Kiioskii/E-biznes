services:
    api:
        build: .
        container_name: echo-gorm-api
        ports:
            - "8080:8080"
        volumes:
            - sqlite_data:/app/data
        environment:
            DB_PATH: /app/data/app.db

volumes:
    sqlite_data:
